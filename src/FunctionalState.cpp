#include "FunctionalState.h"
#include <iostream>

/**
 * @brief Executes the run method in FunctionalState.
 *
 * This function simulates the execution of the functional state and
 * returns true, indicating that the state is operational.
 *
 * @return Always returns true.
 */
bool FunctionalState::run() {
    return true;
}
